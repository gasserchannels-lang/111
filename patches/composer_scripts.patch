diff --git a/composer.json b/composer.json
index 1234567..abcdefg 100644
--- a/composer.json
+++ b/composer.json
@@ -50,6 +50,20 @@
         "test": "phpunit",
         "test-coverage": "phpunit --coverage-html coverage",
         "test-verbose": "phpunit --verbose",
+        "test:single": "phpunit --colors --filter='' --",
+        "test:feature": "phpunit tests/Feature",
+        "test:unit": "phpunit tests/Unit",
+        "test:browser": "phpunit tests/Browser",
+        "phpstan:single": "vendor/bin/phpstan analyse --error-format=raw --paths=%file%",
+        "phpstan:strict": "vendor/bin/phpstan analyse --level=max",
+        "pint:single": "vendor/bin/pint --test %file%",
+        "pint:check": "vendor/bin/pint --test",
+        "pint:fix": "vendor/bin/pint",
+        "eslint:single": "npx eslint %file%",
+        "eslint:check": "npx eslint . --max-warnings=0",
+        "eslint:fix": "npx eslint . --fix",
+        "stylelint:single": "npx stylelint %file%",
+        "stylelint:check": "npx stylelint . --max-warnings=0",
+        "stylelint:fix": "npx stylelint . --fix",
         "analyse": "phpstan analyse",
         "analyse-baseline": "phpstan analyse --generate-baseline",
         "analyse-strict": "phpstan analyse --level=max",
